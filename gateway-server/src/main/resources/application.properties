spring.application.name=gateway-service
eureka.client.eureka-server-port=8761
server.port=8080

spring.jpa.hibernate.ddl-auto=none
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://logindb2.cjmci7dzlmm5.eu-west-2.rds.amazonaws.com:3306/loginDB
spring.datasource.username=admin
spring.datasource.password=sem59_password1234

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Disable accessing services using service name (i.e. gallery-service).
# They should be only accessed through the path defined below.
# Link: https://stackoverflow.com/questions/46317388/zuul-service-name-exposed-instead-of-route-path-only
# zuul.ignored-services=*

# Map path to auth service
zuul.routes.authentication-service.path=/auth/**
zuul.routes.authentication-service.service-id=AUTHENTICATION-SERVICE

# by default, all requests to gallery service for example will start with: "/gallery/"
# what will be sent to the gallery service is what comes after the path defined,
# so, if request is "/gallery/view/1", gallery service will get "/view/1".
# in case of auth, we need to pass the "/auth/" in the path to auth service. so, set strip-prefix to false
zuul.routes.auth-service.strip-prefix=false

# exclude authorization from sensitive headers
zuul.routes.auth-service.sensitive-headers=Cookie,Set-Cookie   